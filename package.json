{
  "name": "apps",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:packages": "npm run build -w @common/contracts",
    "dev": "concurrently \"npm run dev -w client\" \"npm run dev -w server\" \"npm run dev -w packages\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build -w @common/contracts & npm run build -w server"
  },
  "workspaces": [
    "apps/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
